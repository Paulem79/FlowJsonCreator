package io.github.paulem.fjc.flow;

/**
 * Represents a general interface for mods, serving as a common type
 * for different implementations of mod-related objects.
 * <p>
 * Classes implementing this interface are used to encapsulate
 * information about various mod types, such as those from specific
 * mod providers or custom sources. Implementing classes may include
 * details such as project references, download URLs, version
 * information, or file identifiers.
 * <p>
 * This interface allows grouping of mod objects and facilitates
 * operations like adding, removing, or categorizing mods within
 * a collection.
 */
public interface Mod {
}
